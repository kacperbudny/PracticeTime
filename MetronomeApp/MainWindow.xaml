<Window x:Class="MetronomeApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MetronomeApp"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    <Grid>
        <StackPanel x:Name="WindowStackPanel" 
                    Margin="10">
            <Grid Margin="0,0,0,5">
                <StackPanel HorizontalAlignment="Center" 
                            Orientation="Horizontal"  
                            Margin="0,5">
                    <Button x:Name="TempoDownButton"
                            Content="◄"
                            Style="{StaticResource changeTempoButtonsStyle}"
                            Click="TempoDownButton_Click"/>
                    <TextBox x:Name="TempoBox" 
                             Text="120" 
                             Margin="10,0"
                             PreviewTextInput="TempoBox_PreviewTextInput"
                             TextChanged="TempoBox_TextChanged"
                             FontSize="20"
                             Width="100"
                             TextAlignment="Center"/>
                    <Button x:Name="TempoUpButton"
                            Content="►"
                            Style="{StaticResource changeTempoButtonsStyle}"
                            Click="TempoUpButton_Click"/>
                </StackPanel>
            </Grid>

            <Button x:Name="StartButton" 
                    Content="START"
                    FontSize="50" 
                    Click="StartButton_Click"
                    MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" 
                    MinHeight="{Binding ActualWidth, RelativeSource={RelativeSource Self}}"
                    />

            <Grid Margin="0,5,0,5">
                <GroupBox Background="GhostWhite" 
                        BorderBrush="Silver" 
                        BorderThickness="1" 
                        Padding="10,5">
                    <GroupBox.Header>Tap Tempo</GroupBox.Header>
                    <StackPanel HorizontalAlignment="Center" 
                                Orientation="Horizontal">
                        <Button x:Name="CancelTapTempoButton"
                                Content="Cancel"
                                Style="{StaticResource tapTempoButtonsStyle}"
                                Click="CancelTapTempoButton_Click"
                                IsEnabled="False"/>
                        <Button x:Name="TapTempoButton"
                                Content="Tap tempo"
                                Style="{StaticResource tapTempoButtonsStyle}"
                                Click="TapTempoButton_Click"
                                Margin="5"
                                Width="120"/>
                        <Button x:Name="AcceptTapTempoButton"
                                Content="Accept"
                                Style="{StaticResource tapTempoButtonsStyle}"
                                Click="AcceptTapTempoButton_Click"
                                IsEnabled="False"/>
                    </StackPanel>
                </GroupBox>
            </Grid>

            <Grid>
                <GroupBox Background="GhostWhite" 
                        BorderBrush="Silver" 
                        BorderThickness="1" 
                        Padding="10,5"
                        Width="{Binding ActualWidth, ElementName=WindowStackPanel}">
                    <GroupBox.Header>Timer</GroupBox.Header>
                    <StackPanel>
                        <StackPanel HorizontalAlignment="Center" 
                                Orientation="Horizontal"
                                x:Name="TimerStackPanel">
                            <Button x:Name="TimerDownButton"
                                    Content="-"
                                    Style="{StaticResource tapTempoButtonsStyle}"
                                    Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" Click="TimerDownButton_Click"/>
                            <Label x:Name="TimerLabel" 
                                   x:FieldModifier="public"
                                   Content="01:00"
                                   Style="{StaticResource labelStyle}"
                                   Margin="5,0"/>
                            <Button x:Name="TimerUpButton"
                                    Content="+"
                                    Style="{StaticResource tapTempoButtonsStyle}"
                                    Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" Click="TimerUpButton_Click"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" 
                                Orientation="Horizontal"
                                >
                            <Button x:Name="StartTimerButton"
                                    Content="Start timer"
                                    Margin="0,5,0,0"
                                    Style="{StaticResource tapTempoButtonsStyle}"
                                    Width="{Binding ActualWidth, ElementName=TimerStackPanel}" Click="StartTimerButton_Click" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </Grid>

        </StackPanel>
    </Grid>
</Window>
 